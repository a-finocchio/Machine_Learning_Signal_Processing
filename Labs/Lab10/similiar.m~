function S = similiar(T,R)
    [R_row,R_col]
    S = zeros(size(T,1),size(R,1));
    for m = 1:size(T,1)
        for n = 1:size(R,1)
            S(m,n) = sum((T(m,:)-R(n,:)).^2).^0.5;
        end
    end



end